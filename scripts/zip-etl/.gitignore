# Vendored Census source data — fetched on demand by fetch-zcta-gazetteer.sh,
# never committed. Only the fetch script (with its pinned sha256), the ETL,
# the test + fixture, and the generated frontend/public/zip-index.json are
# tracked. CI never runs the fetch; the ETL test runs on the committed fixture.
.cache/

# Generated by build-zip-index.ts each run — the list of centroids that
# resolved to no CONUS state. Operator diagnostic, not a tracked artifact;
# the counts live in SIZE-REPORT.md instead.
dropped.log
