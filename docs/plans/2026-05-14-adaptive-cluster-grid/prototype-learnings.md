Gate 1 (reconcile p99): 3.6ms at 390×844 dark, 10.5ms max across all 10 captures — comfortably under the 16ms budget across viewports and themes (n=44 reconciles per sweep, z=8→z=15 in 0.5 increments).
Gate 2 (marker DOM count): max 31 nodes (30 grid markers + 1 pill at 1920×1080) — two orders of magnitude under the 2,500 ceiling at the 500-row prototype scale; gate is non-binding at this dataset size and will need re-testing against the full 18k AZ corpus in Phase 1.
Gate 3 (hit-extender ≥44/≥48): every sampled hit-zone met the per-pointer minimum once the inset formula was per-axis — the spec's `inset: min(0, (44-markerSize)/2)` had a sign bug AND used a single dimension; 2×1 (52×28) markers need per-axis insets so the short height extends to 44 while the wider width stays at 0.
Gate 4 (console hygiene): zero errors and zero warnings across all 10 captures after fixing favicon 404 (data-URI inline) and source maxzoom (24, must exceed clusterMaxZoom=22).
Surprises: (a) hit-extender inset formula in spec §4.4 needs a sign flip and per-axis split — update Phase 1 task 1.5 accordingly; (b) source maxzoom must be set explicitly to satisfy clusterMaxZoom=22; (c) the dataset is sparse enough at AZ-overview that pills don't reliably appear until z=10+ — Phase 1 perf-gate e2e should target known hotspot clusters (Tucson/Madera/Ash) to actually stress the 4×4 + pill paths.
