-- Up Migration
-- Issue #498. Re-curates the 14 NULL-svg_data family_silhouettes
-- rows against Phylopic build 538 — 3 legacy from migration 17000's
-- operator-skip list (cuculidae, ptilogonatidae, remizidae) and 11
-- from the #495 backfill (migration 34000) where the prior curation
-- pass found no license-compatible vectorFile.
--
-- Generated by scripts/curate-phylopic.mjs --recurate-nulls on 2026-05-13
-- using the Phylopic API two-step recipe (/nodes?filter_name →
-- /images?filter_node) and the existing auto-pick heuristic
-- (license preference CC0 > CC-BY-3.0 > CC-BY-4.0 > CC-BY-SA-3.0,
-- then alphabetic creator, then UUID).
--
-- Rescued (svg_data flipped from NULL → real path-d):
--   (none — build 538 added no usable picks for the 14 NULL families)
--
-- Still NULL after build 538 (rejection reason in parentheses):
--   calcariidae (http-404-on-nodes)
--   cuculidae (no-candidates)
--   gaviidae (http-404-on-images)
--   icteriidae (http-404-on-nodes)
--   numididae (http-404-on-images)
--   peucedramidae (http-404-on-nodes)
--   phasianidae (no-candidates)
--   polioptilidae (http-404-on-nodes)
--   ptiliogonatidae (http-404-on-nodes)
--   ptilogonatidae (http-404-on-nodes)
--   remizidae (http-404-on-nodes)
--   tityridae (http-404-on-nodes)
--   tytonidae (no-candidates)
--   vireonidae (http-404-on-images)
--
-- The full audit trail (every candidate the heuristic considered,
-- per family) lives at scripts/phylopic-picks.json under the updated
-- entries. The skipFamilies memoization mechanism is now empty and
-- deprecated — see the comment at the top of scripts/curate-phylopic.mjs.
--
-- After this migration lands in main, the operator runs
-- scripts/purge-silhouettes-cache.sh (#252) as part of the production
-- deploy runbook to purge the CDN cache for /api/silhouettes.

-- No UPDATEs: build 538 produced zero usable picks. This migration
-- is intentionally a comment-only audit record (with a SELECT 1 to
-- satisfy node-pg-migrate's "Up section must execute" contract).
-- The 14 family rows remain svg_data=NULL; the _FALLBACK consumer
-- (#246) continues to render the generic shape tinted with each
-- family's color. Re-run scripts/curate-phylopic.mjs --recurate-nulls
-- against a newer Phylopic build to pick up future contributions.
SELECT 1;

-- Down Migration
-- Revert ONLY the rescued rows back to NULL. Families that were not
-- rescued in this migration are unchanged.
-- No-op: nothing was rescued, so nothing to revert.
SELECT 1;
