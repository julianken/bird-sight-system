{
  "name": "@bird-watch/_placeholder",
  "version": "0.0.0",
  "private": true,
  "description": "Placeholder workspace so npm's --workspaces flag resolves to >0 targets before real packages land in PR 1c. Remove in PR 1c when shared-types + family-mapping are added.",
  "scripts": {
    "test": "exit 0",
    "lint": "exit 0",
    "build": "exit 0"
  }
}
